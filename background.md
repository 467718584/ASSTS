# 量化交易系统核心概念与术语词典 (Background Reference)

本文档旨在记录本量化选股与择时交易系统的核心术语、命名规范及底层架构逻辑，方便后续复盘、代码维护与系统升级。

## 一、 核心时间与价格术语 (Time & Price Indicators)

在我们的策略中，字母与数字的组合用于精确描述**买卖时机**或**模型的预测目标 (Label)**。

### 1. 时间维度 (Time)
以推荐模型发出买入信号的当天（或特征截断日）为基准 `Day 1`：
* **2**: 指代推荐日后的**第二天 (Day 2)**。通常是我们的建仓/买入日。
* **3**: 指代推荐日后的**第三天 (Day 3)**。通常是我们的平仓/卖出日（超短线 T+1 逻辑）。

### 2. 价格维度 (Price)
* **s (Start)**: 开盘价 (Open)
* **h (High)**: 最高价 (High)
* **v (VWAP/Value)**: 全天均价 (Mean / Volume Weighted Average Price)
* **e (End)**: 收盘价 (Close)

### 3. 经典组合代号与双重含义
这些代号在系统中既可以表示**“选股模型的预测目标”**，也可以表示**“回测时的卖出动作”**。

* **2s3h (买在开盘，预测/卖在最高)**
  * **作为选股模型**: 寻找具备“极强爆发力”的股票。模型预测的是 Day 2 开盘到 Day 3 最高价的涨幅。
  * **实战意义**: 最好练、最容易识别，但回测时难以确保卖在最高点，需要配合动态止盈（如 PPO）来捕捉。
* **2s3v (买在开盘，预测/卖在均价)**
  * **作为选股模型**: 寻找具备“真实资金承接力”的趋势股，过滤掉尾盘偷袭的骗线标的。
  * **作为交易策略**: 是一种非常稳健的回测平仓手段，代表了不具备高频盯盘能力下的真实滑点预期。
* **2s3e (买在开盘，预测/卖在收盘)**
  * **作为交易策略**: 死拿策略。Day 2 开盘买入，无视盘中波动，Day 3 尾盘强制平仓。回撤通常较大，但执行绝对确定。
* **2s3s (买在开盘，卖在开盘)**
  * **作为交易策略**: 隔夜策略。Day 2 开盘买入，Day 3 开盘直接砸盘跑路，仅赚取“隔夜溢价”，规避 Day 3 日内波动的风险。

---

## 二、 模型与回测日志命名规范 (Naming Conventions)

系统的文件命名（如 CSV、日志、模型权重）遵循严格的模板，以实现实验参数的快速溯源。

### 命名模板
`{触底数量}_{特征长度}_{特征数}_{选股模型}-{交易策略}_{损失函数}_{隔离区下限}-{隔离区上限}_{精度指标}_{精度值}-{测试集阈值}-{每日最大选股数}`

### 模板拆解与示例说明
**示例**: `0d_30d_14f_2s3h-2s3e_BCE_0005-003_AUC_05844-05-10.xlsx`

1. **`0d` (触底数量)**: 早期特征保留标记（0天触底等）。
2. **`30d` (特征长度)**: 提取过去多少天的数据作为输入（如 30d, 60d, 120d）。目前结论表明 `30d` 在超短线（T+1）中捕捉动量最为敏锐。
3. **`14f` (特征数)**: 输入的特征维度（Features）。目前包含 14 个量价及技术指标特征。
4. **`2s3h-2s3e` (模型-策略)**: 
   * 前者 `2s3h` 是 Alpha 发动机：使用预测最高价的模型进行选股。
   * 后者 `2s3e` 是执行变速箱：在回测时采用 Day 3 收盘卖出的规则进行测试。
5. **`BCE` (损失函数)**: 使用的 Loss 类型，如 BCE (二元交叉熵) 或 FOCAL (Focal Loss 解决样本不平衡)。
6. **`0005-003` (隔离区 Buffer Zone)**: `0.005` 到 `0.03`。代表在训练时，剔除了涨幅在 0.5% 到 3% 之间的微涨/震荡样本（视为噪音），强迫模型只学习真正的“大涨”和“走弱”特征。
7. **`AUC` (精度指标)**: 监控并保存最优模型的指标。我们摒弃了受阈值影响极大的 F1，转向衡量模型全局排序能力的 AUC。
8. **`05844` (精度值)**: 测试集上的真实表现。如 0.5844 (即 58.44% 的 AUC)。
9. **`05` (测试集阈值)**: 回测买入阈值，即 `0.5`。模型输出概率 > 0.5 才会买入。*(注：测试证明 0.8 阈值过于苛刻导致资金利用率低，0.5 配合均价卖出能实现收益最大化)*。
10. **`10` (每日最大选股数)**: 资金管理的仓位限制，每天最多将资金等分为 10 份买入 Top 10 的股票。

---

## 三、 核心架构论与系统哲学 (Architecture & Philosophy)

本系统的最终形态确立为**“预测与执行解耦的两阶段架构 (Two-Stage Pipeline)”**：

### Stage 1: 分类模型 (Alpha Generator / The参谋)
* **任务**: 发现具有上涨潜力的股票池。
* **最佳实践**: `LSTM + Attention` 网络，使用 `30d` 窗口，`2s3h` 为 Label 训练。
* **逻辑**: 它是 HR，通过宏观日线特征过滤掉显性的垃圾股，将“具有冲高爆发力”的股票放入观察池。

### Stage 2: 强化学习模型 (PPO Execution / The狙击手)
* **任务**: 在给定的优质股票池中，决定具体的盘中卖出时机。
* **状态设计 (State)**: 摒弃绝对价格，采用纯比例特征（实时浮盈、距离历史最高点回撤 drawdown_from_peak、时间衰减比例 time_decay 等）。
* **动作空间 (Action)**: [0: 持有, 1: 卖出]。取消买入动作（默认已在 Day 2 开盘持仓），专注卖出专家。
* **奖励机制 (Reward)**: 触发极度亏损（如 -3%）时给予强烈惩罚（强平），日常持仓给予微小惩罚（防死扛），卖出时结算真实盈亏比。

### 关键认知 (Insights)
1. **盈亏同源与幸存者偏差**: 训练 PPO 时，**必须**使用 Stage 1 筛选出的“全量股票”（包含假突破的亏损股）。只有让 PPO 见识过“看似要涨实则大跌”的伪装股，它才能学会在实盘中**挥刀止损**。
2. **AUC > F1**: 选股本质上是一个“排序问题（Ranking）”而非绝对的“分类问题（Classification）”。我们更关心“这 10 只股票是不是全市场最好的”，而不是跨过某条绝对的 0.5 分数线。
3. **好模型 + 笨策略 = 赚钱；笨模型 + 好策略 = 保本**：通过高爆发力的 `2s3h` 模型配合最死板的均价卖出 `2s3v`，已经证明了 Alpha 的存在。PPO 的引入是为了突破收益天花板。


## 四、 数据集与衍生数据命名规范 (Dataset Naming Conventions)

为了区分原始数据、预打包的分类模型训练集、分类模型筛选后的衍生数据，以及最终的回测测试集，我们的数据集文件需遵循以下极其严格的命名规范，从物理层面杜绝“未来函数 (Data Leakage)”。


### 1. 预打包分类模型训练集 (Pre-packaged Classification Dataset)
在进行 Stage 1 (分类模型) 训练前，经过特征工程处理、滚动窗口切片，并剔除了缓冲区噪音样本的定型数据集。
* **命名模板**: `{触底标记}-m-data-{特征窗口}-preprocess-{特征数}-{预测目标}-class-{隔离区下限}_{隔离区上限}`
* **模板拆解与示例**: 
  * **示例 1**: `0d-m-data-120d-preprocess-14f-2s3v-class-0005_003`
  * **示例 2**: `0d-m-data-120d-preprocess-14f-2s3h-class-001_003`
  * **`0d-m-data`**: 项目早期特征前缀与主数据流标记。
  * **`120d`**: 时间步长 (Time Steps)，即向后回溯的滚动窗口大小 (如 30d, 60d, 120d)。
  * **`preprocess`**: 明确标识这是经过标准化/归一化及缺失值处理的就绪数据。
  * **`14f`**: 包含的特征维度数 (Features)，目前为 14 个量价指标。
  * **`2s3v` / `2s3h`**: 该数据集内部计算好的 Label (预测目标)。
  * **`class`**: 数据集任务类型，代表用于分类任务 (Classification)。
  * **`0005_003`**: 缓冲区/隔离区范围 (Buffer Zone)。代表剔除了涨幅在 0.5% 到 3% (或 1% 到 3%) 之间的模糊样本，强制模型学习极端特征。

### 2. 回测/实盘测试数据集 (Backtest / Test Dataset)
专门用于评估模型在未知数据（通常为 2024-2025 年）上真实表现的数据集。
* **命名模板**: `{触底标记}-m-data-{特征窗口}-preprocess-{特征数}-{预测目标}-class-{参数}-TEST`
* **示例**: `0d-m-data-120d-preprocess-14f-2s3e-class-003-TEST`
* **核心纪律**: 任何带有 `-TEST` 后缀的数据，**绝对禁止**出现在任何分类模型或 PPO 模型的 `.train()` 代码路径中。它只能被 `DataLoader` 用于推理评估或接入回测系统。

### 3. PPO 专属提纯股票池 (Filtered PPO Dataset)
当使用 Stage 1 的分类模型对历史数据进行预测，并提取出 `Score > 阈值` 的“提纯数据”时，必须在命名中体现**筛选此数据集所用的模型版本和阈值**。
* **命名模板**: `ppo_pool_{时间跨度}_{周期级别}_by_{模型代号}_{选股阈值}`
* **示例**: `ppo_pool_2019-2023_min1_by_30d2s3h05_0.5.csv` (或文件夹)
  * **`ppo_pool`**: 明确标识这是专门给 PPO 训练用的提纯股票池。
  * **`by_30d2s3h05`**: 产生这批数据的“HR（分类模型）”是谁。
  * **`0.5`**: 实际采用的截断阈值（Score > 0.5）。
* **逻辑意义**: 防止数据错配。用 `2s3h` 模型选出来的股票池，绝对不能拿去训练一个试图在 `2s3e` 逻辑下运行的 PPO 环境。**: 任何带有 `test` 前缀的数据，**绝对禁止**出现在任何模型的训练代码路径中（防止未来函数泄露 Data Leakage）。